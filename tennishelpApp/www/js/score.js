// This is where the javascript for score keeping will go

  // Adding the name of the Players in the scoreboard

  if ({{player1.name}} = true) {
   result = {{player1.name}};
  } else {
     result = "player 1";
  }


  if ({{player2.name}} = true) {
   result = {{player2.name}};
  } else {
     result = "player 2";
  }
